/// A class that holds the IDs for the menu items.
class MenuIds {
  /// The ID for the new file menu item.
  static const int newFile = 0;

  /// The ID for the open file menu item.
  static const int openFile = 1;

  /// The ID for the save menu item.
  static const int save = 2;

  /// The ID for the export menu item.
  static const int export = 3;

  /// The ID for the canvas size menu item.
  static const int canvasSize = 4;

  /// The ID for the settings menu item.
  static const int settings = 5;

  /// The ID for the platforms menu item.
  static const int platforms = 6;

  /// The ID for the about menu item.
  static const int about = 10;
}
